rename:
  send_ts: send_ts_gen
  accept_bank: accept_bank_gen
  accept_user: accept_user_gen
  client_id: client_id_gen

features:
  categorical: [country, hour_bucket]
  numeric: [age, app_intensity]
  time:
    all_generated: [hour, dow, day, hour_sin, hour_cos, dow_sin, dow_cos,
                    day_sin, day_cos, hour_bucket, is_weekend, is_payday_zone]