import pandas as pd
import numpy as np

def create_time_features(df: pd.DataFrame, ts_column: str = 'send_ts') -> pd.DataFrame:
    # Твой полный код create_time_features из блокнота
    # (вставь сюда)
    pass

def build_time_grid() -> pd.DataFrame:
    # Твой полный код build_time_grid из блокнота
    # (вставь сюда)
    pass